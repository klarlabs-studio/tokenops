---
updated: 2026-07-03
note: append-only log — never edit or delete entries; supersede with "→ superseded [date]"
---
- 2026-07-03: Adopted Agent OS memory system — persistent cross-session state via memory/ + wiki/ + cadence skills.
- 2026-07-03: `fmt` formatters guarantee critical-line survival in the ENGINE (`enforceCritical`), not per-formatter — so user config formatters are exactly as safe as built-ins. This is the moat: rules can be data, the invariant lives in code.
- 2026-07-03: Learning is offline + advisory + gated, never runtime self-modification. `fmt learn --apply` only writes loss-level overrides (safe — never touches critical rules); new-formatter candidates are printed, not auto-written. Preserves determinism.
- 2026-07-03: Cloud CLIs (aws/gcloud/az) pass JSON through untouched — the generic consecutive-dup scrub corrupts structured output. Only table/text is compressed.
- 2026-07-03: Config formatters excluded from the proxy content-sniff plane (no reliable content signature); they only run when dispatched by explicit command token.
- 2026-07-03: Skipped low-value RTK-parity commands (ls/cat/find/grep/diff/wget) — mostly signal, little to compress; the generic scrub covers them safely.
- 2026-07-03: Commit the Agent OS memory system (memory/ + wiki/ + AGENTS.md) to the repo rather than gitignore — cross-machine continuity for a solo-maintained OSS project; reversible if it becomes noise for contributors.
- 2026-07-03: Proxy-plane validation done via a default-pipeline integration test (real Anthropic tool_result → command_fmt event with savings), not live traffic — proves the wiring; live-traffic validation deferred, not blocking.
- 2026-07-03: read-guard observe-first, active opt-in — a PreToolUse hard-block on re-reads risks degrading agent flow, so default is observe (log only); active only after the user sees real reclaimable numbers. PreToolUse can only allow/deny (not modify input), so block-or-allow; conservative = unchanged full same-session re-reads only.
- 2026-07-03: The honest outcome of the whole token investigation: for this user, none of the three levers (caveman output prose <1%, fmt command output ~1-4% of Bash, Read re-reads ~0 reclaimable) yields much — because their usage is already efficient. The value delivered was measurement/diagnostics that proved where tokens go and honestly reported the absence of easy wins, not a fabricated saving.
- 2026-07-03: Full provider coverage via a shared OpenAI-compatible framework (`NewOpenAICompatible`), not per-provider files — most upstreams are OpenAI-wire-format, so 8 providers became ~1 line each + pricing/tokenizer registration; proxy went 4→13. Normalizer parameterized by id so each meters with its own provider (not a shared "openai"). Cohere added first-class (dedicated v1+v2 normalizer) since Bearer auth suits the passthrough proxy; Bedrock deferred (needs SigV4, no per-provider auth hook).
- 2026-07-03: opencode passive reader built on SQLite (reads ~/.local/share/opencode/opencode.db mode=ro), the first non-JSONL vendor source — because opencode persists sessions in SQLite (verified on real machine). Gemini CLI passive reader NOT built: its logs.json is a prompt log with no token data (verified) — honest boundary, proxy plane only. These boundaries are the tradeoff of local-first/no-telemetry, documented not papered over.
- 2026-07-03: `provider set <name>` validates against known providers + supports preset URLs (omit URL) — an unknown name previously hard-crashed the daemon at boot (BuildProviderRoutes rejects unknown override keys). Fail early in the CLI instead.
- 2026-07-04: The prediction reads the vendor's OWN rate-limit meter, not a message count. The pollers already stored five_hour_used_pct/primary_used_pct/percent_remaining + resets in event attributes but nothing read them, and countsAsMessage excluded exactly those high-signal granularities — so session_budget could say "continue" at 87% with "high" confidence. Added an optional Authoritative window to ComputeSessionBudget/ComputeHeadroom + latestAuthoritativeWindow/Monthly (mcp). Wired across session_budget, plan_headroom window, and plan_headroom monthly (Copilot/Cursor percent_remaining/used_pct). This was THE core-value gap.
- 2026-07-04: Exact OpenAI tokenizer via tiktoken-go/tokenizer v0.7.0 (offline, embedded vocab), NOT v0.8.0 — v0.8.0 forces go 1.26 but CI pins 1.25; v0.7.0 needs only 1.23. Overrides the OpenAI heuristic via Registry.Set with the heuristic as fallback. OpenAI-compat providers (groq etc.) keep the heuristic — o200k would be MORE wrong for llama-family tokenizers.
- 2026-07-04: Optimizer savings estimate must tokenize the REAL before/after content, not a canary (strings.Repeat("a ", n) returned ~bytes/4 and undercounted dense payloads). retrieval_prune scored 0.6 (below the 0.85 gate) because it's a blind positional guess with no relevance signal. dedupe is lexical Jaccard, not semantic — doc corrected (no embedding seam exists).
- 2026-07-04: Deferred plan-catalog tier additions (xAI SuperGrok, Perplexity Pro, etc.) — each needs a real dated vendor SourceURL + accurate caps; fabricating caps violates the honest-measurement ethos the whole product stands for.
- 2026-07-04: Released v0.34.0 (vendor-meter predictions, exact tiktoken tokenizer, 17 providers, optimizer honesty, read-guard cross-agent fix). Assets + brew 0.34.0, clean release.
- 2026-07-04: Over-time charts built as a real tokenops feature (`fmt analyze --svg` → tokens/volume/composition-over-time), NOT a blog-only hack — two dependency-free svgchart primitives (Lines, StackedArea) + weekly bucketing in jsonlfmt (PeriodBucket, parses per-turn timestamp + message.usage). Weekly not monthly because the on-disk Claude Code JSONL only spans ~5 weeks.
- 2026-07-04: Blog embeds composition + tokens over-time; keep the 800:1 headline (from `tokenops spend`) and show NO axis numbers on the line chart, so the fmt-analyze ~490:1 measurement doesn't contradict it. Charts captioned "week by week over recent weeks" to coexist honestly with the "six months" prose (recent-window zoom, not a span claim). The honest through-line: the over-time data STRENGTHENED the post (ratio + composition are stable every week, not an averaging artifact).
- 2026-07-04: Published a second blog post, "The tool was guessing" (the rate-limit prediction bug), as a sequel to 800:1. Self-critical about our own product (found+fixed reads as strength). Honesty guardrail: every code-level claim is a verified commit; the illustrative 87% is labeled as such (vendor pollers weren't enabled on this machine, so no such event literally exists in the store) and the stats were reframed to verified facts. Fixed the 800:1 post's "six months"→"several weeks" after verifying the events store spans ~5-6 weeks (numbers 32.5B/40.4M/804:1 all confirmed).
- 2026-07-04: `fmt analyze --svg` gained a `--charts` selector (all/bars/timeline/ids) backed by a single chartDef registry (one source of truth for output + validation). Cut v0.35.0 (over-time charts + --charts). Caught+fixed a periodLabel bug (axis showed raw week keys) and regenerated the already-live blog charts.
- 2026-07-07: ADR 0001 (docs/adr/0001-usage-coaching-hooks.md, Accepted) — opt-in, hook-driven, budget-based coaching that makes the retrospective coach/scorecard engines live. Established docs/adr/.
- 2026-07-07: Coach-hook alerting = **cumulative per-session $-budget + graduated latched alerts** (50/75/100% + over-budget escalation), NOT a flat per-turn threshold. Rationale, data-driven from real transcripts: sessions run 7–9k turns at ~609k cache-read/turn — under any sane per-turn line — yet cost >$1,800 cumulatively; the per-turn model fundamentally misses long-*flat* sessions. Default budget $50 (median session $107, max $2,397). $-normalized so it's model-agnostic (fable/haiku accrue slower against the same tiers). Fires only the single highest newly-crossed tier per Stop. `--threshold`/`--cooldown` replaced by `--budget`.
- 2026-07-07: Claude Code hooks are **fail-open, always** — any error → exit 0, no output. The Stop hook emits `{"systemMessage":…,"suppressOutput":true}` (user-facing, non-blocking) and NEVER `decision:"block"`. Reads only the transcript TAIL (~256 KiB), never the whole multi-MB file. Mirrors the read-guard pattern (internal/infra/<name> + a cobra command whose bare form is the handler + hook/stats subcommands).
- 2026-07-07: `tokenops hooks install/uninstall/status` scaffolder — idempotent merge into ~/.claude/settings.json by an args[0] marker, .bak backup + atomic write, refuses to overwrite malformed JSON, warns on binary/version mismatch. Install hooks via an explicit stable binary path (a `go install` dev build shadowed homebrew on PATH and mis-wired the hooks until removed).
- 2026-07-07: Released v0.36.0 (usage-coaching hooks, Phase 1 + 1.1). goreleaser + brew tap, clean.
- 2026-07-07: Ingestion staleness surfaces as a `warnings` signal, NOT a `blocker`. Blockers are config-level hard gates (providers_unconfigured); a stalled poller is a soft runtime signal — `tokenops status` degrades `state` ready→degraded but keeps `ready:true` and keeps serving. 48h window (vs vendor-usage status's 24h) to avoid nagging on a day of non-use; honest caveat that 0 events can be legitimate non-use. Store-interface-based + nil-safe (`config.CheckStaleIngestion`). #131.
- 2026-07-07: Released v0.37.0 (stale-ingestion health warning). goreleaser + brew tap, verified live (flags anthropic-cookie, not the resumed claude_code_jsonl).
- 2026-07-07 → SUPERSEDED 2026-07-07 (WRONG — see the reversal below): Opus 4.x pricing corrected to $15/$75/$1.50 (was $5/$25/$0.50 = ⅓; cache-read couldn't be fixed alone since it's 10% of input — the whole row was ⅓). Released in v0.38.0 (#135).
- 2026-07-07: ADR 0002 Accepted + BUILT (v0.38.0) — pricing is researched (pluggable LiteLLM source), snapshots are timestamped+sourced, cost is effective-dated (each event priced at the rate in effect at its timestamp), refresh DIFFS so drift is loud, and a consistency guard (cache≈10%/output≈5×) lints every snapshot. Phase 1 (#137) framework + Phase 2 (#138) effective-dated engine. spend must NOT import pricing (primitive keyed by time.Time in spend; snapshot→table conversion in pricing). PromptEvent has no Timestamp — occurrence time is on the Envelope (threaded via ComputeAt).
- 2026-07-07: REVERSAL — the Opus "$15/$75/$1.50 correction" above was WRONG. Anthropic CUT Opus pricing at 4.5: Opus 4/4.1 = $15/$75/$1.50, but Opus 4.5-4.8 = $5/$25/$0.50 (the catalog's ORIGINAL value). Confirmed by the LiteLLM feed via `tokenops pricing refresh` (network works from the machine). Reverted to $5/$25/$0.50 in v0.39.0 (#141/#142). The consistency guard can't catch this ($5 and $15 are both internally consistent — cache=10%, output=5x); only the source can. My earlier $-usage analyses were 3x too high (~$50.6k/7d → ~$17k). This is ADR 0002 working exactly as designed.
- 2026-07-07: Broaden the ADR 0002 pricing source to ALL 10 catalog providers (#147), keying snapshots `<provider>/<model>`; keep the cache≈10%/output≈5× ratio guard Anthropic-only (other providers get a generic cache≤input sanity check). Multiplexers (fireworks/together/openrouter) excluded — a static card can't price namespaced models.
- 2026-07-07: On dated-SKU collisions, pick the NEWEST DATED snapshot, not `-latest` and not the lexically-first (oldest) id (#148). Rationale: LiteLLM's `-latest` aliases go stale (codestral-latest = old $1/$3 vs current codestral-2508 = $0.30/$0.90); dated snapshots are precise and immutable. `-latest` is the fallback only when no dated SKU exists.
- 2026-07-08: Correct Mistral/DeepSeek/o1/grok-3 catalog rates against vendor pages (#149) — Mistral keys track the current -latest generation (Large 3 $0.50/$1.50, Medium 3.5 $1.50/$7.50, Small 4 $0.15/$0.60); deepseek-chat/reasoner → v4-flash $0.14/$0.28 cache $0.0028 (deprecated aliases, retire 2026-07-24); o1 +cache $7.50; grok-3 +cache $0.75. Every row cross-checked against the vendor, never single-sourced.
- 2026-07-08: Fix adapter collision edges (#150) — gate 4-digit date suffixes to plausible YYMM so OpenAI MMDD isn't misordered; reject SKU-qualifier boundaries (fast/mini/lite/…) so distinct tiers don't fold into a base key. Both were producing false drift the runtime snapshot applied over the baseline.
- 2026-07-08: Snapshot-vs-baseline precedence — the runtime effective engine layers fetched snapshots over baseline (latest wins for current events), so a stale source silently regresses a hand-verified rate. Chose VERIFIED-ROW PINNING (#151): `verified: true` rows are stripped from snapshot overrides so baseline wins; unpinned rows still auto-adopt. Rejected "baseline-authoritative for current" (would retire auto-adopt) and "document-only" (keeps the footgun). Pinned the non-Anthropic vendor-verified rows — the gap the Anthropic-only guard leaves.
- 2026-07-08: Released v0.41.0 (pricing arc #147–#152) — tag → goreleaser → brew tap. Two feat + fixes → minor bump from v0.40.0.
- 2026-07-08: CI — PRs always run required checks (dropped paths-ignore on the pull_request trigger; kept on push) so doc/release PRs report and merge without --admin (#154). Also restored the test job's single-OS matrix so it reports "Test (ubuntu-latest)", matching a stale branch-protection required context that was silently forcing --admin on every merge.
- 2026-07-08: CI — gated the 5-way cross-compile build matrix to `github.event_name=='push'` (#155). It was ~55% of a run and redundant per-PR (go test/vet compile on linux; goreleaser cross-compiles at release). Saves ~5 min/PR; main+release keep full coverage. Chosen over reverting #154's PR-runs-CI, to keep no-admin doc PRs.
- 2026-08-18: Supervise `tokenops start` via an installable unit (`tokenops daemon install`), not a placeholder plist. Detection (v0.43.0) without prevention still lost ingestion on reboot. launchd + systemd --user; `--no-load` for tests. Brew caveats point at the same command.
- 2026-08-18: `daemon.url` is 0600 because it carries `dashboard_token`. 0644 was a local auth bypass on a shared host.
- 2026-08-18: Gemini 2.5 cache-read corrected to 10% of input ($0.125 / $0.03 / $0.01) against https://ai.google.dev/gemini-api/docs/pricing (Vertex agrees). The $0.31 / $0.075 catalog values were the old explicit-cache SKU. Added flash-lite so the longer prefix wins. Gemini 1.5 left unverified — gone from the vendor page.
- 2026-08-18: Retention is opt-in (`retention.keep`); empty default deletes nothing. The pruner is wired in `tokenops start` when keep is set. Coaching async pipeline stays unwired (on-demand `tokenops coach` / `replay` only) — auto-start would replay sessions in the background.

