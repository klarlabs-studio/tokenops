---
date: 2026-08-18
---
# Session 2026-08-18

General evaluation of v0.43.0 on `main`, then fixed the findings.

## Evaluation
- Product engines (fmt critical-line, dated pricing, vendor-meter prediction) are solid.
- v0.43.0 made the 27-day ingestion outage *visible* but did not prevent it: launchd template still had `__TOKENOPS_BIN__` placeholders; Linux had no unit.
- `daemon.url` was 0644 with `dashboard_token` inside.
- MCP/dashboard still said `tokenops up`.
- archlint listed ~28 of ~51 context packages (AGENTS.md contract already broken).
- Published docs still pointed at the old formula tap.
- Gemini 2.5 cache-read unverified; catalog had stale explicit-cache figures.

## Shipped on this branch
- `tokenops daemon install/uninstall/status` (launchd + systemd --user)
- `daemon.url` mode 0600
- `tokenops up` → `tokenops start`
- archlint complete + anti-drift tests
- Gemini 2.5 Pro/Flash/Flash-Lite vendor-verified and pinned
- opt-in `retention.keep` wired in the daemon
- docs/Pages/SECURITY/CONTRIBUTING/README aligned with the cask + current minor
